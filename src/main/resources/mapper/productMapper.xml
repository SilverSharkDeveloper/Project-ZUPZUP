<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.floc.mapper.ProductMapper">
    <insert id="insert">
        INSERT INTO TBL_PRODUCT
        (ID, PRODUCT_NAME, PRODUCT_DETAIL, PRODUCT_COST, PRODUCT_STOCK,  PRODUCT_CATEGORY, PRODUCT_USED_LOCATION, PRODUCT_IMAGE_NAME, PRODUCT_IMAGE_UUID, PRODUCT_IMAGE_SIZE)
        VALUES(SEQ_PRODUCT.NEXTVAL,#{productName}, #{productDetail}, #{productCost}, #{productStock}, #{productCategory} , #{productUsedLocation} , #{productImageName},#{productImageUuid}, #{productImageSize})
    </insert>

    <select id="selectOneByProductId" resultType="productVO">
        SELECT ID, PRODUCT_NAME, PRODUCT_DETAIL, PRODUCT_COST, PRODUCT_STOCK, PRODUCT_REGISTER_DATE, PRODUCT_UPDATED_DATE, PRODUCT_CATEGORY, PRODUCT_USED_LOCATION, PRODUCT_IMAGE_NAME, PRODUCT_IMAGE_UUID, PRODUCT_IMAGE_SIZE
        FROM TBL_PRODUCT
        WHERE ID = #{productId}
    </select>

    <select id="selectAll" resultType="productVO">
        SELECT ID, PRODUCT_NAME, PRODUCT_DETAIL, PRODUCT_COST, PRODUCT_STOCK, PRODUCT_REGISTER_DATE, PRODUCT_UPDATED_DATE, PRODUCT_CATEGORY, PRODUCT_USED_LOCATION, PRODUCT_IMAGE_NAME, PRODUCT_IMAGE_UUID, PRODUCT_IMAGE_SIZE
        FROM TBL_PRODUCT
    </select>

    <delete id="delete">
        DELETE FROM TBL_PRODUCT WHERE ID = #{productId}
    </delete>
</mapper>